[project]
name = "experiment-templates"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "mlflow>=3.14.0",
    "pandas>=2.3.3",
    "pyfixest>=0.60.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.15.21",
]
